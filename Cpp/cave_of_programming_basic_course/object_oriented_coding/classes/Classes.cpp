#include "Cat.h"

// define the prototype here also solves the problem
// but we should define the prototypes on the header file
//void speak();

int main(void) {
    Cat cat;

    cat.speak();
    cat.jump();

    //return 0;
}